.research
# Internal delivery notes — run plans, retros, session state. Local-only since the repo
# went public; the shipped history is what's public, not the working notes behind it.
.deliver/
.scratch/
.work/
.swarm/
node_modules/
dist/
# Built plugin hook bundles (generated by tsup from src/hooks/*.ts in run 2).
# Anchored and extension-scoped on purpose: a bare `hooks/` also matches `src/hooks/`,
# which silently un-tracks the hook sources. `/hooks/hooks.json` stays committed.
/hooks/*.mjs
.DS_Store
*.swp
*.swo
*~
.env.local
coverage/

# VitePress build output and cache for the marketing/docs site
site/.vitepress/dist/
site/.vitepress/cache/

# Skill-owned local state (auto-land ledger etc.)
.elderfo/
